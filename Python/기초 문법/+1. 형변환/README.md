## 🔄 데이터 형변환 (Type Conversion)

데이터의 타입을 다른 타입으로 바꾸는 것을 의미합니다.

### 1. 암시적 형변환 vs 명시적 형변환

| 구분 | 설명 | 예시 | 결과 타입 |
|:---:|:---|:---|:---:|
| **암시적**<br>(Implicit) | 파이썬이 **자동으로** 수행.<br>데이터 손실이 없는 경우 발생 (정수 $\rightarrow$ 실수). | `3 + 3.5` | `float` (6.5) |
| **명시적**<br>(Explicit) | 사용자가 **직접** 함수를 써서 변환.<br>데이터 손실이 발생할 수 있음 (실수 $\rightarrow$ 정수). | `int(3.5)` | `int` (3) |
```python
# 암시적 형변환
# 정수(int)와 실수(float)의 덧셈
print(3 + 5.0)  # 8.0 -> 실수(float)로 형변환

# 불리언(bool)과 정수(int)의 덧셈
print(True + 3)  # 4 -> True는 1로 형변환
# 불리언간의 덧셈
print(True + False)  # 1 -> True는 1, False는 0으로 형변환
```


### 2. 주요 형변환 함수 (명시적)

| 함수 | 설명 | 사용 예시 | 결과 |
|:---:|:---|:---|:---|
| `int()` | 정수(Integer)로 변환 | `int(3.9)` | `3` (소수점 버림) |
| `float()` | 실수(Float)로 변환 | `float(3)` | `3.0` |
| `str()` | 문자열(String)로 변환 | `str(123)` | `'123'` |
| `bool()` | 불리언(Boolean)으로 변환 | `bool(1)` | `True` |
| `list()` | 리스트(List)로 변환 | `list('abc')` | `['a', 'b', 'c']` |
| `tuple()` | 튜플(Tuple)로 변환 | `tuple([1, 2])` | `(1, 2)` |

