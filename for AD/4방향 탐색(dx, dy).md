
# 📝 TIL: 2차원 배열의 나침반, 델타 탐색 (`dx`, `dy`)

델타 탐색(Delta Search)은 2차원 배열 문제의 **구구단**과 같습니다. 처음엔 낯설지만 원리를 그림으로 이해하면 정말 쉽다.
**날짜**: 2026. 02. 06 (금)
**주제**: 2차원 배열의 나침반, 델타 탐색 (`dx`, `dy`)

## 1. 델타(Delta)란 무슨 뜻일까?

수학이나 과학에서 **델타()**는 **'변화량'**을 의미한다.
즉, `dx`, `dy`는 **"현재 위치에서 X좌표와 Y좌표가 얼마나 변해야 하는가?"**를 저장해둔 리스트다.

> **핵심 목표**: "상, 하, 좌, 우"를 `if`문 4개로 따로따로 짜지 않고, **`for`문 하나로 우아하게 처리**하기 위함이다.

---

## 2. 좌표계의 비밀 (이걸 알아야 안 헷갈림!)

우리가 흔히 쓰는 수학 그래프와 **프로그래밍의 2차원 배열**은 방향이 조금 다르다.

* **수학**: 위로 올라가면 가 증가한다.
* **컴퓨터 배열**: **아래로 내려가야 행(Row) 인덱스가 증가한다.**

이 차이 때문에 `dx`, `dy` 값을 설정할 때 실수가 많이 나온다.

### 배열의 좌표 기준 (`arr[행][열]`)

보통 알고리즘에서는 `arr[x][y]`라고 할 때:

* **`x`**: **행(Row, 세로 줄)** 번호 (0, 1, 2...)
* **`y`**: **열(Col, 가로 줄)** 번호 (0, 1, 2...)

---

## 3. 방향별 변화량 분석 (상세 해설)

현재 내 위치가 **중앙 `(2, 2)`**라고 가정해보자. 여기서 4방향으로 한 칸씩 움직인다면 좌표는 어떻게 변할까?

### ① 상 (Up)

* **동작**: 위로 한 칸 간다.
* **변화**: 줄(행) 번호가 하나 **줄어든다**. 칸(열) 번호는 그대로다.
* **식**: `행 - 1`, `열 + 0`
* **델타**: `dx = -1`, `dy = 0`

### ② 하 (Down)

* **동작**: 아래로 한 칸 간다.
* **변화**: 줄(행) 번호가 하나 **늘어난다**. 칸(열) 번호는 그대로다.
* **식**: `행 + 1`, `열 + 0`
* **델타**: `dx = 1`, `dy = 0`

### ③ 좌 (Left)

* **동작**: 왼쪽으로 한 칸 간다.
* **변화**: 줄(행) 번호는 그대로다. 칸(열) 번호가 하나 **줄어든다**.
* **식**: `행 + 0`, `열 - 1`
* **델타**: `dx = 0`, `dy = -1`

### ④ 우 (Right)

* **동작**: 오른쪽으로 한 칸 간다.
* **변화**: 줄(행) 번호는 그대로다. 칸(열) 번호가 하나 **늘어난다**.
* **식**: `행 + 0`, `열 + 1`
* **델타**: `dx = 0`, `dy = 1`

---

## 4. 이것만 외우자! (코드 패턴)

이 리스트를 정의하는 순간, 우리는 **내비게이션**을 손에 넣은 것이다.

```python
# 방향 정의: [상, 하, 좌, 우] 순서 (순서는 바뀌어도 짝만 맞으면 됨)
# x는 행(세로), y는 열(가로)의 변화량
dx = [-1, 1, 0, 0] 
dy = [0, 0, -1, 1]

```

### 짝꿍 확인하기

* `dx[0]`, `dy[0]` -> `(-1, 0)` -> **상**
* `dx[1]`, `dy[1]` -> `(1, 0)` -> **하**
* `dx[2]`, `dy[2]` -> `(0, -1)` -> **좌**
* `dx[3]`, `dy[3]` -> `(0, 1)` -> **우**

---

## 5. 전체 코드 시뮬레이션

이제 실제로 `for`문을 돌리면서 움직여보자.

```python
# 1. 맵 크기 (5x5)
N = 5 
arr = [[0]*N for _ in range(N)]

# 2. 현재 내 위치 (가운데)
x, y = 2, 2

# 3. 델타 배열 선언 (상하좌우)
dx = [-1, 1, 0, 0]
dy = [0, 0, -1, 1]

print(f"현재 위치: ({x}, {y})")

# 4. 4방향 탐색 시작 (k는 0, 1, 2, 3)
for k in range(4):
    # 다음 위치(Next x, Next y) 계산
    nx = x + dx[k] 
    ny = y + dy[k]
    
    # 5. [매우 중요] 범위 체크 (Boundary Check)
    # 맵 밖으로 나가는지 검사해야 함. 
    # (0보다 작아지거나, N보다 크거나 같아지면 안 됨)
    if 0 <= nx < N and 0 <= ny < N:
        print(f"방향 {k}로 이동 -> 좌표: ({nx}, {ny})")
    else:
        print(f"방향 {k}는 낭떠러지! 이동 불가.")

```

---

## 6. 꿀팁: 변수명이 헷갈린다면?

`x`, `y`라고 쓰면 수학 좌표계랑 헷갈려서 "어? x가 가로 아닌가?" 하는 순간 뇌정지가 올 수 있다.
그럴 땐 **`r` (row, 행)**, **`c` (column, 열)** 변수명을 쓰면 훨씬 직관적이다.

```python
# dr: 행의 변화량 (위아래)
# dc: 열의 변화량 (좌우)
dr = [-1, 1, 0, 0]
dc = [0, 0, -1, 1]

# 현재 위치 (r행 c열)
for k in range(4):
    nr = r + dr[k]
    nc = c + dc[k]
    
    if 0 <= nr < N and 0 <= nc < N:
        # 로직 수행
        pass

```

### 요약

1. **`dx`는 행(세로) 변화**, **`dy`는 열(가로) 변화**다. (또는 `dr`, `dc` 사용)
2. `nx = x + dx[k]`, `ny = y + dy[k]` 공식처럼 쓴다.
3. **범위 체크(`if 0 <= nx < N...`)** 없으면 `IndexError`로 프로그램 터진다. 꼭 안전벨트 매듯이 작성하자.