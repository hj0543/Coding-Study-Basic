
# 📂 IM 핵심 정복: 문자열 처리 & 카운팅 (DAT)

파이썬의 강력한 문자열 함수(`find`, `replace`, 슬라이싱)를 잘 쓰는 것도 중요하지만,
**"데이터의 빈도수(개수)를 얼마나 효율적으로 세는가?"** 가 IM의 숨겨진 핵심이다.

## 1. 문자열 필수 스킬 (String Manipulation)

### 🔹 회문 (Palindrome) 검사

앞뒤가 똑같은 단어 찾기. 슬라이싱 `[::-1]`을 쓰면 한 방에 해결된다.

```python
word = "LEVEL"
if word == word[::-1]:
    print("회문입니다.")

```

> **수동 구현**: `left`, `right` 포인터를 양 끝에 두고 좁혀오며 비교하는 로직도 알아두자. (알고리즘적 사고)

### 🔹 패턴 찾기

긴 문장(`T`) 안에 특정 단어(`P`)가 있는지 찾기.

```python
T = "I love Python Programming"
P = "Python"

# 1. in 연산자 (가장 쉬움)
if P in T:
    print("Exist")

# 2. find 함수 (인덱스 반환, 없으면 -1)
idx = T.find(P) 

# 3. 직접 구현 (IM 스타일 - 2중 for문)
# 슬라이딩 윈도우처럼 P의 길이만큼 잘라서 비교
N, M = len(T), len(P)
for i in range(N - M + 1):
    if T[i : i + M] == P:
        print(f"{i}번 인덱스에서 발견")

```

---

## 2. DAT (Direct Address Table) - 빈도수 세기

**"값(Value)을 인덱스(Index)로 사용한다"**는 테크닉.
`Dictionary`를 써도 되지만, 숫자의 범위가 작다면 **리스트(배열)**를 쓰는 것이 훨씬 빠르고 직관적이다.

### 🔹 개념 원리

* 데이터: `[1, 5, 1, 2, 5, 5]`
* 목표: 각 숫자가 몇 번 나왔는지 세라.
* **DAT**: 크기가 넉넉한(예: 6) 배열을 만들고, 숫자가 나올 때마다 해당 인덱스의 값을 `+1` 한다.

### 📝 코드 패턴 (숫자 카운팅)

```python
data = [1, 5, 1, 2, 5, 5]
# 숫자의 최대값이 5이므로 크기 6짜리 배열 생성 (0~5 인덱스)
counts = [0] * 6 

for num in data:
    counts[num] += 1 

# 결과 확인
print(counts) # [0, 2, 1, 0, 0, 3]
# 1이 2번, 2가 1번, 5가 3번 나왔다는 뜻

```

### 🔹 응용: 문자열 카운팅 (아스키 코드 활용)

문자 `A`, `B`, `C`... 의 빈도수를 셀 때는 `ord()` 함수를 이용해 문자를 숫자로 바꾼다.

* `ord('A')`는 65이다.
* 보통 `ord(char) - ord('A')`를 해서 0번 인덱스부터 차곡차곡 쌓는다.

```python
text = "BANANA"
# 알파벳 대문자 26개 카운팅 배열
counts = [0] * 26 

for char in text:
    # 'A'를 0번 인덱스로 매핑
    idx = ord(char) - ord('A') 
    counts[idx] += 1

# 가장 많이 나온 문자 찾기
max_cnt = 0
max_char = ''
for i in range(26):
    if counts[i] > max_cnt:
        max_cnt = counts[i]
        max_char = chr(i + ord('A')) # 다시 문자로 복구

print(f"가장 많은 문자: {max_char} ({max_cnt}개)") # A (3개)

```

---

## 3. 실전 팁 & 주의사항

1. **DAT 배열 크기**: 문제에서 숫자의 범위가 `0 ~ 100`이면 `counts = [0] * 101`로 만든다. 범위가 `0 ~ 1,000,000`처럼 너무 크면 딕셔너리(`dict`)를 써야 한다. (메모리 초과 주의)
2. **누적합 (Prefix Sum)**: DAT와 함께 자주 쓰이는 기법. 특정 구간의 합을 빠르게 구할 때 사용한다.
* 예: `2559번 수열` 문제에서 10만 개의 온도 데이터 중 연속된 K일의 합을 구할 때, 매번 더하면 시간 초과. 슬라이딩 윈도우나 누적합을 써야 한다.



### 💡 추천 문제: [BOJ 2559 수열] (Silver 3)

* **핵심**: 슬라이딩 윈도우 (윈도우를 한 칸 밀 때, `새로운 값 더하고` `맨 앞의 값 빼기`)

```python
# 슬라이딩 윈도우 기초 (DAT 아님, 카운팅/합 응용)
K = 2
temps = [3, -2, -4, -9, 0, 3, 7]
# 초기 K일의 합
current_sum = sum(temps[:K])
max_sum = current_sum

# 한 칸씩 이동하며 갱신 (O(N))
for i in range(len(temps) - K):
    # 다음 값 더하고, 이전 값 빼기
    current_sum = current_sum + temps[i+K] - temps[i]
    if current_sum > max_sum:
        max_sum = current_sum

```