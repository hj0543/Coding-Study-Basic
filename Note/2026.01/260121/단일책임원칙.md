
# 🛠️ 단일 책임 원칙 (Single Responsibility Principle)

**"하나의 함수(또는 클래스)는 반드시 하나의 기능만을 수행해야 한다."**
깔끔하고 유지보수가 쉬운 코드를 작성하기 위한 가장 기본적인 소프트웨어 설계 원칙이다.

## 1. 함수 설계 원칙

함수를 설계할 때 다음 3가지 핵심 요소를 고려한다.

* **명확한 목적:** 함수는 오직 한 가지 작업만 수행하며, 함수 이름만 보고도 그 목적을 명확히 알 수 있어야 한다.
* **책임 분리:** 데이터의 검증, 실제 연산(처리), 결과 저장(또는 출력) 등의 단계를 각각 별도의 함수로 분리한다. 각 함수는 독립적으로 동작 가능해야 한다.
* **유지보수성:** 코드를 작은 단위의 함수로 나누어 관리함으로써, 수정이 필요할 때 다른 부분에 미치는 영향을 최소화한다.

## 2. 코드 비교 (Bad vs Good)

### ❌ 나쁜 예 (책임 중복)

데이터 필터링, 계산, 출력이라는 3가지 서로 다른 작업을 하나의 함수 안에서 모두 처리한다.

* 재사용이 어렵고, 로직 수정 시 함수 전체를 건드려야 한다.

```python
def process_data(data):
    # 1. 데이터 필터링 (책임 1)
    filtered_data = [d for d in data if d > 0]
    
    # 2. 데이터 합계 계산 (책임 2)
    total = sum(filtered_data)
    
    # 3. 결과 출력 (책임 3)
    print(f"Total: {total}")

```

### ✅ 좋은 예 (책임 분리)

각 함수가 하나의 역할만 수행하도록 분리한다.

* `get_filtered_data`: 필터링만 담당
* `calculate_total`: 계산만 담당
* `display_result`: 출력만 담당
* **장점:** 필요한 기능을 레고 블록처럼 조합하여 사용할 수 있다.

```python
def get_filtered_data(data):
    """데이터 필터링 역할"""
    return [d for d in data if d > 0]

def calculate_total(data):
    """계산 역할"""
    return sum(data)

def display_result(result):
    """출력 역할"""
    print(f"Total: {result}")

# 메인 로직: 함수 조합 사용
raw_data = [1, -2, 3, -4, 5]
filtered = get_filtered_data(raw_data)
total = calculate_total(filtered)
display_result(total)

```

---

### ✅ 시도해 볼 것

단일 책임 원칙을 적용했으니, 이제 **함수의 유효 범위(Scope)와 변수의 수명(Life Cycle)**에 대해 정리해볼 차례다. 지역 변수와 전역 변수가 함수 내에서 어떻게 동작하는지 복습해보자.