
# 📝 패킹(Packing)과 언패킹(Unpacking)

여러 개의 데이터를 하나의 컬렉션으로 묶거나, 반대로 풀어내는 과정을 의미한다.

### 1. 패킹 (Packing)

여러 개의 값을 하나의 변수나 매개변수에 **묶어서 담는 것**이다.

* **변수 할당 시:**
* 우변에 나열된 값들이 자동으로 **튜플(Tuple)**로 묶여 좌변 변수에 할당된다.


```python
numbers = 1, 2, 3, 4, 5
print(numbers)  # (1, 2, 3, 4, 5) -> 튜플 타입

```


* **함수 정의 시 (`*args`):**
* 매개변수 앞에 `*`를 붙이면, 호출 시 전달된 위치 인자들을 모두 모아 하나의 **튜플**로 받는다.


```python
def sum_all(*args):
    print(args)  # (1, 2, 3) -> 튜플로 패킹됨
    return sum(args)

sum_all(1, 2, 3)

```



### 2. 언패킹 (Unpacking)

컬렉션(리스트, 튜플 등)에 묶여 있는 값들을 **개별 요소로 풀어내는 것**이다.

* **변수 할당 시:**
* 좌변의 변수 개수와 우변의 요소 개수가 **정확히 일치**해야 한다.


```python
packed_data = (10, 20, 30)
a, b, c = packed_data  # 10, 20, 30이 각각 a, b, c에 할당된다.

```


* **함수 호출 시 (`*`):**
* 인자로 전달할 리스트나 튜플 앞에 `*`를 붙이면, 괄호를 벗겨내고 **값만 풀어서** 함수에 전달한다.


```python
def add(x, y):
    return x + y

numbers = [10, 20]
add(*numbers)  # add(10, 20)과 동일하게 실행된다.

```



### 3. `*` (Asterisk)와 `**` (Double Asterisk) 비교

상황(정의 vs 호출)과 데이터 타입(시퀀스 vs 매핑)에 따라 역할이 다르다.

| 기호 | 대상 | 패킹 (함수 정의 시) | 언패킹 (함수 호출 시) |
| --- | --- | --- | --- |
| **`*`** | **리스트, 튜플** | 여러 인자를 **튜플**로 묶음 (`*args`) | 리스트 요소를 **값**으로 풀어서 전달 |
| **`**`** | **딕셔너리** | 키워드 인자를 **딕셔너리**로 묶음 (`**kwargs`) | 딕셔너리를 **키워드 인자**(`key=value`)로 풀어서 전달 |

**예시 (딕셔너리 언패킹):**

```python
def print_info(name, age):
    print(f"이름: {name}, 나이: {age}")

user = {'name': 'Alice', 'age': 30}
print_info(**user)  # print_info(name='Alice', age=30)으로 변환되어 실행된다.

```

### 4. 활용 Tip: 남은 요소 할당 (Extended Unpacking)

언패킹 시 `*`를 변수 앞에 붙이면, 나머지 요소들을 모두 모아 **리스트**로 할당한다.

```python
numbers = [1, 2, 3, 4, 5]

first, *rest, last = numbers

print(first) # 1
print(rest)  # [2, 3, 4] -> 중간 요소들이 리스트로 묶인다.
print(last)  # 5

```

---

### ✅ 시도해 볼 것

이해를 돕기 위해 **패킹/언패킹을 활용하여 두 변수의 값(Value)을 서로 바꾸는(Swap) 코드**를 작성해보자.